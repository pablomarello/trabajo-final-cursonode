<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ejemplo products</title>
</head>
<body>
    <%- include('partials/header') %> 
    <h1>Product list</h1>
    <ul>
        <% products.forEach(product => { %>
            <li><%= product.name %> - <%= product.price %></li>
        <% }); %>
    </ul>
</body>
</html>